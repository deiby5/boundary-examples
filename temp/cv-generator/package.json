{
  "name": "boundary-temp-cv-generator",
  "version": "1.0.0",
  "description": "Generate synthetic technology CV PDFs for Boundary examples.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "generate": "npm run build --silent && node dist/cli.js",
    "smoke": "npm run build --silent && node dist/smoke-test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "pdfkit": "^0.15.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/pdfkit": "^0.13.9",
    "typescript": "^5.7.3"
  }
}
